<script type="text/javascript">
$(function() {
    makeTabActive("mailbox");
    });

</script>



				
				



				<!-- Charts -->
				<div class="g_12">
					
					<h2>Listing my mailboxes</h2>

				<table id="table-mailboxes" class="tables ui-tabs-panel ui-widget-content ui-corner-bottom ui-tabs-hide">
				  <tr>
				    <th>Name</th>
				    <th>Print</th>
				    <th>Edit</th>
				    <th>Delete</th>
				  </tr>
				
				<% @mailboxes.each do |mailbox| %>
				  <tr>
				    <td><%= mailbox.name %></td>
				    <td><%= link_to('Print sticker', path_to_image("../"+mailbox.qrcode)) %></td>
				    <td><%= link_to 'Edit', edit_mailbox_path(mailbox) %></td>
				    <td><%= link_to 'Delete', mailbox, method: :delete, data: { confirm: 'Are you sure?' } %></td>
				  </tr>
				<% end %>
				</table>

					
				</div>
				

				<div class="g_6 contents_options">
						 <%= link_to 'Add Mailbox', new_mailbox_path, :class=>"simple_buttons" %> 
					
				</div>

